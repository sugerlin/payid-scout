# Contributing to PayID Scout

感谢参与。这个项目的目标是帮助用户发现具有稀缺性、品牌价值和通用意义的 PayID，不是批量收集公司名单或自动抢注名称。

## 提交代码前

```bash
npm run check
```

如果修改了扩展界面，请在 Chrome 中重新加载 `extension` 文件夹，并在 `https://cloudflare.pay/` 上手动验证：

- 扩展可以打开和关闭；
- 输入框仍然能正常查询；
- AVAILABLE / RESERVED 状态能正确记录；
- 本地缓存不会重复查询；
- 不会点击 Reserve。

## 词库贡献

欢迎提交跨领域、真实、有明确含义的英文词。优先考虑短、易读、易拼、易记、适合多个行业的通用词。不要提交公司名单、专门为某个公司准备的品牌清单或个人敏感信息。

商标关联可以作为参考字段，但不应成为自动删除候选的理由。

## Pull Request 建议

- 说明修改目的和影响范围；
- 保持本地运行，不新增不必要的网络请求；
- 不加入自动注册、自动点击 Reserve 或绕过频率限制的逻辑；
- 给界面改动附上截图或复现步骤。
